    <div class="text-center">
        <h1 class="text-3xl font-bold mb-4">Read To-Do</h1>

        <!--<?php echo "<pre>"; ?>
        <?php print_r($this->tasks); ?>
        <?php echo "</pre>"; ?>
        <?php 
            if (isset($this->tasks)) {
                echo "tasks està definit.<br>";
                if (!empty($this->tasks)) {
                    echo "tasks no està buit.<br>";
                } else {
                    echo "tasks està buit.<br>";
                }
            } else {
                echo "tasks no està definit.<br>";
            }

            if (!empty($this->tasks) && is_array($this->tasks)) {
                echo "Les tasques estan correctament definides i són un array.";
            } else {
                echo "No és un array o està buit.";
            }
        ?> -->

    <?php if (!empty($this->tasks) && is_array($this->tasks)): ?>
        <p>Dades trobades!</p>
        <div class="mt-4 p-4 bg-gray-100 rounded">
        <h2 class="text-xl font-semibold">To-Do List</h2>
        <?php foreach ($this->tasks as $task): ?>
            <p>Estic dins del loop</p>
            <form class="space-y-4">

                <div class="mb-4">
                    <input type="text" name="user" class="border border-gray-300 p-2 rounded w-full" 
                        value="<?= htmlspecialchars($task['user']) ?>" />
                </div>

                <div class="mb-4">
                    <input type="text" name="title" class="border border-gray-300 p-2 rounded w-full" 
                        value="<?= htmlspecialchars($task['title']) ?>" />
                </div>

                <div class="mb-4">
                    <input type="text" name="status" class="border border-gray-300 p-2 rounded w-full" 
                        value="<?= htmlspecialchars($task['status']) ?>" />
                </div>

                <div class="mb-4">
                    <label for="start-date" class="block text-sm font-medium text-gray-700">Start Date</label>
                    <input type="date" id="start-date" name="start-date" class="border border-gray-300 p-2 rounded w-full" 
                        value="<?= htmlspecialchars($task['start_date']) ?>" />
                </div>

                <div class="mb-4">
                    <label for="end-date" class="block text-sm font-medium text-gray-700">End Date</label>
                    <input type="date" id="end-date" name="end-date" class="border border-gray-300 p-2 rounded w-full" 
                        value="<?= htmlspecialchars($task['end_date']) ?>" />
                </div>

            </form>

            <hr class="my-4">
        <?php endforeach; ?>
        </div>
    <?php else: ?>
        <p class="text-red-500 mt-4">No tasks found.</p>
    <?php endif; ?>
        
        <a href="<?php echo WEB_ROOT ?>" class="inline-block mt-6">
            <button class="flex items-center justify-center w-12 h-12 bg-blue-600 text-white rounded-full hover:bg-blue-700 transition-all duration-300 ease-in-out">
                <svg class="w-6 h-6 transform transition-transform hover:rotate-360 duration-500" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M11 17l-5-5m0 0l5-5m-5 5h12"></path>
                </svg>
            </button>
        </a>
    </div>